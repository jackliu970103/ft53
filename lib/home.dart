import 'package:flutter/material.dart';

class homepage extends StatelessWidget{
  Widget build(BuildContext context){
    return Scaffold(
      body: Padding(padding: EdgeInsets.all(50),child:
        Column(
          children: [

          ],
        ),),
    );
  }
}